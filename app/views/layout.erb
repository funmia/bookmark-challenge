<!DOCTYPE html>
<html>
  <head>
    <title>Bookmark Manager</title>
  </head>

  <body>
    <% if @user %>
      <h2>Welcome <%= @user.email %></h2>
    <% end %>
    <% if flash[:errors] && !flash[:errors].empty? %>
        Please refer to the follow errors below:
        <ul id='errors'>
          <% flash[:errors].each do |error| %>
          <li><% error %></li>
          <% end %>
        </ul>
      <% end %>
    <%= yield %>
  </body>
</html>
